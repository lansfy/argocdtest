apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

resources:
  - cronjob.yaml

configMapGenerator:
  - name: time-printer-script
    files:
      - print_time.py
